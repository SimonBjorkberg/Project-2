<div
  class="py-4 my-5 sm:w-3/4 w-5/6 m-auto border-teal-700 border-2 rounded-md"
>
  <p class="text-teal-700 text-center text-2xl font-bold">{{topic.title}}</p>
</div>

{{#each thread}}
  <section
    class="p-4 rounded-md mb-4 w-4/5 sm:w-2/3 m-auto hover:bg-zinc-300 hover:border-teal-500 transition-colors border-teal-500 border"
  >
    <div onclick="threadClick('{{this._id}}')" class="cursor-pointer">
      <div>
        <h2 class="text-2xl text-teal-900 font-bold mb-1">{{this.title}}</h2>
        <p>{{thread.likes.length}}</p>
      </div>
      <p>{{this.content}}</p>
      <div class="flex sm:flex-row mt-2 justify-between flex-col">
        <div class="flex items-center">
          <img
            src="{{this.author.profilePicture}}"
            alt="PP"
            class="w-8 h-8 rounded-full mr-2"
          />
          <p>Posted by
            <span class="font-semibold">
              <a
                href="/profile/{{this.author.username}}"
                class="text-teal-900 hover:underline"
              >
                {{this.author.username}}
              </a>
            </span>
          </p>
        </div>
        <div class="flex items-center">
          <p>Posted: {{this.createdAt}}</p>
        </div>
      </div>
    </div>
  </section>
{{/each}}

{{#if userInSession}}
  <div class="bg-teal-900 p-4 rounded-md mb-4 sm:w-1/2 w-4/5 mx-auto my-32">
    <form action="/threads/create/{{topic._id}}" method="post">
      <div class="grid grid-cols-1 gap-4">
        <div>
          <input
            type="text"
            id="title"
            name="title"
            placeholder="Title"
            required
            class="w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:border-teal-500"
          />
        </div>
        <div>
          <textarea
            id="content"
            name="content"
            placeholder="Content"
            required
            class="w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:border-teal-500 resize-y"
          ></textarea>
        </div>
      </div>
      <div class="mt-4">
        <button
          type="submit"
          class="bg-teal-700 text-white py-2 px-4 rounded-md hover:bg-teal-600 hover:border-teal-600 transition-colors"
        >
          Create Thread
        </button>
      </div>
    </form>
  </div>
{{/if}}